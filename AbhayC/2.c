#include<stdio.h>
int main()
{
    int m,n ;
    printf("enter a values of m and n:");
    scanf("%d%d", &m, &n);
    if (m==n)
        printf("m and n are equal");
     else
    printf("m and n are not equal");
}