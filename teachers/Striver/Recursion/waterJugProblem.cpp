#include<bits/stdc++.h>
using namespace std;
int a=4,b=3,target=2;
void fun(int x,int y,vector<vector<int>>vis)
{
    vis[x][y]= 1;
    if(x<a && !) fun(a,y,vis);
    if
}
int main()
{
    vector<vector<int>>vis(a+1,vector<int>(b+1,-1));
    fun(0,0,vis);
}